Campo de data — digitação livre com máscara automática dd/mm/aaaa, com popover de calendário opcional para seleção visual. Nunca usar `<input type="date">` nativo.

```jsx
<DatePicker label="Data de entrada" value={value} onChange={setValue} required />
```
