Dropdown select, e.g. choosing galpão, lote, or fornecedor. It uses the same compact 44px treatment and 2px required-state indicator as Input.

```jsx
<Select label="Galpão" options={[{value:'1',label:'Galpão 1'}]} value={v} onChange={fn} />
```
