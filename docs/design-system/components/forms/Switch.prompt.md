Toggle switch for binary settings (e.g. Sim/Não, Ativo/Inativo). It uses a 40×24px pill track with an 18px circular thumb. Never use Select for a binary choice; Select is reserved for three or more options.

```jsx
<Switch label="Alertas ativos" checked={v} onChange={setV} />
```
